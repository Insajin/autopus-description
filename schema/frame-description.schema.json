{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autopus.co/schema/figma/frame-description.schema.json",
  "title": "Frame Description Entry",
  "description": "Single Figma frame description entry. SPEC-FIGMA-001 v0.1.0. Persona tag membership for each field is encoded inline within its description using the canonical marker [persona_tags: <comma-list>] per spec.md section 4.1.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "screen_id",
    "display_id",
    "title",
    "intent",
    "user_value",
    "success_criteria",
    "states",
    "edge_cases",
    "component_refs",
    "data_io",
    "design_tokens",
    "variants",
    "navigation",
    "confidence",
    "intent_mismatch",
    "source_hash",
    "write_target",
    "persona_tags",
    "token_usage"
  ],
  "properties": {
    "screen_id": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_-]{1,63}$",
      "description": "Unique screen identifier within a manifest. ASCII upper-case identifier, 2 to 64 chars total. [persona_tags: pm, designer, dev, qa]"
    },
    "display_id": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_-]{1,63}$",
      "description": "Display surface identifier, may equal screen_id when there is a single display. [persona_tags: pm, designer, dev, qa]"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable frame title. UTF-8, no language enforcement at schema level. [persona_tags: pm, designer, dev, qa]"
    },
    "intent": {
      "type": "string",
      "minLength": 0,
      "description": "One-line summary of the frame's intent. PM-facing free text in project language. Empty string allowed when SPEC-FIGMA-003 anti-hallucination (REQ-09) blanks the field. [persona_tags: pm, designer, dev, qa]"
    },
    "user_value": {
      "type": "string",
      "minLength": 0,
      "description": "User value statement: what the user gains from this screen. Empty string allowed when SPEC-FIGMA-003 anti-hallucination (REQ-09) blanks the field. PM review and retrospective surface. [persona_tags: pm]"
    },
    "success_criteria": {
      "type": "string",
      "minLength": 0,
      "description": "Acceptance criterion observable by PM. Empty string allowed when SPEC-FIGMA-003 anti-hallucination (REQ-09) blanks the field. PM review and retrospective surface. [persona_tags: pm]"
    },
    "states": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Enumerated visual or behavioral states (loading, empty, error, populated, etc.). [persona_tags: qa, dev]"
    },
    "edge_cases": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Edge cases and exceptional flows for QA scenario authoring and Dev branch coverage. [persona_tags: qa, dev]"
    },
    "component_refs": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "References to design-system surfaces or product component roles. Avoid exact code module names unless supplied by the project brief. [persona_tags: designer, dev]"
    },
    "data_io": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Product-level data coordination points, required values, events, filters, reset rules, cache/staleness, analytics intent, and permission behavior. Avoid exact API, DB, enum, or module names unless supplied by the project brief. [persona_tags: dev, qa]"
    },
    "area_annotations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["area_id", "title", "target_area", "description"],
        "properties": {
          "area_id": {
            "type": "string",
            "pattern": "^[0-9]+(-[0-9]+)*$",
            "description": "Number shown beside the referenced UI region, e.g. 1 or 2-1. [persona_tags: pm, designer, dev, qa]"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "Short label for the numbered region. [persona_tags: pm, designer, dev, qa]"
          },
          "target_area": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable UI region being annotated, not a code component name. [persona_tags: designer, dev, qa]"
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "Product behavior and policy for this region. [persona_tags: pm, designer, dev, qa]"
          },
          "interaction": {
            "type": "string",
            "minLength": 1,
            "description": "Expected user interaction behavior at product level. [persona_tags: designer, dev, qa]"
          },
          "motion": {
            "type": "string",
            "minLength": 1,
            "description": "Motion or transition expectation without prescribing implementation libraries. [persona_tags: designer, dev, qa]"
          },
          "policy": {
            "type": "string",
            "minLength": 1,
            "description": "Business or UX rule for the region. [persona_tags: pm, qa]"
          },
          "states": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "Region-level states and triggers. [persona_tags: qa, dev]"
          },
          "data_refs": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "References to data_requirements.data_id values. [persona_tags: dev, qa]"
          },
          "qa_notes": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "Region-level QA checks. [persona_tags: qa]"
          },
          "placement_hint": {
            "type": "string",
            "minLength": 1,
            "description": "Approximate canvas placement hint for the numbered callout. [persona_tags: designer]"
          }
        }
      },
      "description": "Numbered UI-region annotations for side-of-frame handoff documentation. Optional and additive. [persona_tags: pm, designer, dev, qa]"
    },
    "data_requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["data_id", "name", "purpose", "required_values"],
        "properties": {
          "data_id": {
            "type": "string",
            "minLength": 1,
            "description": "Stable product-level identifier referenced by area_annotations.data_refs. [persona_tags: dev, qa]"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Data requirement name. [persona_tags: pm, dev, qa]"
          },
          "purpose": {
            "type": "string",
            "minLength": 1,
            "description": "Why the screen or region needs this data. [persona_tags: pm, dev, qa]"
          },
          "required_values": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "Required values or field groups at product level. [persona_tags: dev, qa]"
          },
          "source": {
            "type": "string",
            "minLength": 1,
            "description": "Coordination source such as user input, permission, policy, or external content. Not an endpoint/table name unless provided. [persona_tags: dev, qa]"
          },
          "refresh_policy": {
            "type": "string",
            "minLength": 1,
            "description": "Freshness, cache, or refresh expectation. [persona_tags: dev, qa]"
          },
          "permission": {
            "type": "string",
            "minLength": 1,
            "description": "Access or visibility policy. [persona_tags: pm, dev, qa]"
          },
          "empty_state": {
            "type": "string",
            "minLength": 1,
            "description": "Expected behavior when required data is unavailable. [persona_tags: pm, qa]"
          },
          "notes": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "Additional product-level coordination notes. [persona_tags: pm, dev, qa]"
          }
        }
      },
      "description": "Data list needed to implement or QA the annotated regions, written as product coordination points rather than implementation design. Optional and additive. [persona_tags: pm, dev, qa]"
    },
    "design_tokens": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Design tokens applied (color, spacing, typography). Design system change-detection surface. [persona_tags: designer]"
    },
    "variants": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Component or layout variants in use. Design system change-detection surface. [persona_tags: designer]"
    },
    "navigation": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Navigation targets and entry transitions for this frame. [persona_tags: pm, designer, dev, qa]"
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Generator confidence score in the closed interval [0.0, 1.0]. Review-routing signal. [persona_tags: pm, designer]"
    },
    "intent_mismatch": {
      "type": "boolean",
      "description": "True when generator detected a likely intent mismatch (designer escalation flag). Review-routing signal. [persona_tags: pm, designer]"
    },
    "source_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{8,128}$",
      "description": "Lower-case hex digest of the source frame inputs. Algorithm is defined by SPEC-FIGMA-002. Operator metadata for staleness detection. [persona_tags: pm]"
    },
    "write_target": {
      "type": "string",
      "enum": ["annotation_card", "descriptions_page", "frame_name", "plugin_data", "native_annotation", "none"],
      "description": "Surface the description should be written back to. Operator metadata. [persona_tags: pm]"
    },
    "persona_tags": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["pm", "designer", "dev", "qa"]
      },
      "uniqueItems": true,
      "minItems": 1,
      "description": "Per-frame audience tags asserted by the producer. Schema-level persona membership for each field is encoded inline in field descriptions per spec.md section 4.1. Operator metadata. [persona_tags: pm]"
    },
    "token_usage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["input_tokens", "output_tokens"],
      "properties": {
        "input_tokens": {
          "type": "integer",
          "minimum": 0,
          "description": "Input tokens consumed by the generator for this frame. Telemetry slot; budget enforcement is SPEC-FIGMA-003."
        },
        "output_tokens": {
          "type": "integer",
          "minimum": 0,
          "description": "Output tokens produced by the generator for this frame. Telemetry slot; budget enforcement is SPEC-FIGMA-003."
        }
      },
      "description": "Generator token telemetry. Operator metadata. [persona_tags: pm]"
    },
    "stale": {
      "type": "boolean",
      "default": false,
      "description": "True when source_hash differs from the baseline manifest entry for the same screen_id (REQ-05). Operator metadata. [persona_tags: pm]",
      "$comment": "stale derivation field per REQ-05; persona group: operator metadata (extension of spec.md § 4)"
    },
    "review_status": {
      "type": "string",
      "enum": ["approved", "pending_review"],
      "description": "Post-hoc review state set by SPEC-FIGMA-003 prompt-injection detector. 'pending_review' marks an entry the detector flagged as suspect; the entry is retained for PM audit rather than dropped. Optional, additive in v0.2.0. [persona_tags: pm]"
    },
    "open_questions": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Unresolved product/QA decisions the reader must confirm, surfaced separately from the body. Each item is a plain question or stated gap, not an inline tag. [persona_tags: pm, dev, qa]"
    }
  }
}

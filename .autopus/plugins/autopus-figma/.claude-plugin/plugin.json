{
  "name": "autopus-figma",
  "version": "0.5.0",
  "description": "Autopus Figma description workflow for Claude Code — bundles the autopus-figma MCP server (stdio), the v4 figma-annotation-handoff and figma-desc-review skills, and the figma-desc-reviewer agent so one install wires them.",
  "mcpServers": {
    "autopus-figma": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/../../../dist/src/daemon/mcp-stdio-entry.js"],
      "env": {
        "FIGMA_TOKEN": "${user_config.figma_token}",
        "AUTOPUS_AUDIT_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  },
  "skills": "./skills",
  "agents": "./agents",
  "userConfig": {
    "figma_token": {
      "type": "string",
      "title": "Figma personal access token",
      "description": "Figma personal access token. Stored in secure storage, never written to settings.json or this manifest.",
      "sensitive": true,
      "required": false
    }
  }
}

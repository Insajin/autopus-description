# Workspace Context

> Generated by `@auto setup --auto` on 2026-05-08.

## Root Role

The repository root is the orchestration package `@autopus/figma-read`. It owns shared schemas, root CLIs, generation/read runtime, daemon/MCP runtime, tests, fixtures, and npm workspace wiring.

## Workspaces

`package.json` declares:

```json
["apps/*", "packages/*"]
```

Current workspace members:

- `apps/review-ui`
- `packages/write-router`
- `packages/escalation`
- `tools/validate-manifest`

## Generated And Runtime Paths

Ignored/generated paths include:

- `node_modules/`
- `dist/`
- `.audit/`
- `coverage/`
- `**/.next/`
- `**/tsconfig.tsbuildinfo`
- `.autopus/telemetry/`
- `.autopus/daemon.pid`
- `.mcp.json`
- `audit.log`
- `plugin-bridge-inbox.jsonl`
- `tests/integration/figma-007/.tmp/`

The `.autopus/project/*.md` context files, selected SPEC-FIGMA-008 docs, and the Windows stdio probe matrix are durable context fixtures and are explicitly unignored in `.gitignore`.

## Vendor Boundary

`vendor/cursor-talk-to-figma-mcp/` is a pinned copy of external plugin/MCP bridge code. Root TypeScript excludes `vendor/**`. Avoid broad refactors, formatting churn, or first-party architectural assumptions inside this directory.

## Nested Repository Status

No nested `.git` directories were observed during setup. The vendored package is a copied source tree, not an active nested git repository.

## Safe Editing Policy

- Prefer small, targeted edits in the owning module.
- Do not revert unrelated local changes.
- Keep first-party source files below the project hard limit of 300 lines.
- Preserve MCP public tool/resource names and ordering unless a SPEC explicitly changes them.
- Preserve token redaction and plugin-confirmation gates on all write paths.

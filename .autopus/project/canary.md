# Canary Context

> Generated by `@auto setup --auto` on 2026-05-08.

## Purpose

Canary checks should prove that the repository is installed, type-checkable, testable, buildable, and able to exercise deterministic sample paths without live Figma, Slack, Anthropic, or OpenAI services.

## Baseline Canary

Run from the repository root:

```bash
node --version
npm --version
npm ci
npm run lint
npm test
npm run build
```

Expected:

- Node major version is 22 or newer.
- Dependencies install from `package-lock.json`.
- TypeScript no-emit check passes.
- Vitest passes within configured coverage thresholds.
- `dist/` contains built CLI bins with shebangs.

## Validator Canary

```bash
cd tools/validate-manifest
npm install
npm run build
node dist/index.js ../../samples/dogfood-30frame.manifest.json
```

Expected:

- Exit code is 0.
- stdout ends with a pass summary.
- stderr is empty for the dogfood sample.

## Fixture CLI Canary

After the root build:

```bash
node dist/src/cli.js dogfood-001 --fixture fixtures/30-frame-mock/frames.json --dry-run
```

Expected:

- Exit code is 0.
- Output lists frame size estimates.
- Output contains no raw Figma or bearer token patterns.

## MCP Stdio Canary

After `npm run build`:

```bash
node dist/src/daemon/mcp-stdio-entry.js
```

Expected:

- Process starts as a long-running stdio MCP server.
- It does not print the `autopus-daemon start` boot JSON line.
- External MCP initialize emits one `client_profile_attached` audit row.

## MCP HTTP Canary

After `npm run build`:

```bash
node dist/src/daemon/mcp-http-entry.js --port=0
```

Expected:

- stdout prints `{"event":"http_listening",...}`.
- Address is loopback by default.
- `/mcp` accepts MCP initialize requests and rejects invalid session reuse.

## Current Setup Status

Resolved after remediation: dependencies are installed, `npm run lint` passes, `npm run build` passes, and `npm test` passes on Node `v24.14.1` / npm `11.14.0`.
